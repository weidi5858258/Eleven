com.cyanogenmod.eleven.utils.PopupMenuHelper
com.cyanogenmod.eleven.locale.HanziToPinyin
com.cyanogenmod.eleven.locale.LocaleUtils
上面三个文件有代码被注释掉的

我的设想是：
创建一个播放器实例，等到要播放哪首歌曲或者文件夹里的歌曲时，传递有关信息给播放器进行播放

com.cyanogenmod.eleven.MusicPlaybackService
提供aidl文件给其他组件或者应用使用，里面调的方法是MultiPlayer中的方法，
MultiPlayer中又是调用的MediaPlayer中有关播放，暂停的方法。

关于SessionId
一个Session就是一个会话。每个会话都有一个独一无二的Id来标识。该Id的最终管理在AudioFlinger中。
一个会话可以被多个AudioTrack对象和MediaPlayer共用。
共用一个Session的AudioTrack和MediaPlayer共享相同的AudioEffect。
创建一个AudioTrack对象，必须指定一个SessionId，并与其他使用该SessionId的AudioTrack和MediaPlayer共享AudioEffect。
如果不指定SessionId，将会自动生成一个SessionId，AudioEffect会将该SessionId与新创建的AudioTrack对象关联起来。

MTP主机已停止运行